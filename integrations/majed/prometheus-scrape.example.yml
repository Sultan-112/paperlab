# Merge these jobs into Majed's existing Prometheus config; do not replace it.
scrape_configs:
  # When Prometheus or its collector runs on the demo VM host:
  - job_name: paperlab-demo
    scrape_interval: 5s
    metrics_path: /metrics
    static_configs:
      - targets: ["127.0.0.1:9180"]
